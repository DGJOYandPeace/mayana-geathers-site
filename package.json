{
  "name": "mayana-geathers-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "mayanageathers.com \u2014 static site (Eleventy) deployed on Cloudflare Pages.",
  "scripts": {
    "start": "eleventy --serve --port=8080",
    "dev": "eleventy --serve --port=8080",
    "build": "eleventy",
    "deploy": "eleventy && wrangler deploy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "image-size": "^2.0.2",
    "wrangler": "^4.131.1"
  }
}
